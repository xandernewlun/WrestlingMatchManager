package matchmanager;

public class CommandNames extends CurMatch{
	/*
	 * Command Names will use information from
	 * CurMatch to make easy to remember commands
	 * Such as taking AddPoint(2, "red") and turning it to 
	 * Red-Takedown.
	 */
}
